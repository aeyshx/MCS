# Development notes

Ad-hoc map and network experiments previously stored at the repository root were removed during the cleanup. Keep future one-off experiments in this directory and promote stable checks into `tests/` as pytest tests.
